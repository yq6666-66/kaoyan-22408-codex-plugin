# 安全政策

## 支持版本

当前支持 `1.0.x`。安全修复会在最新补丁版本中发布。

## 架构边界

本项目只发布静态 Skills、共享契约与品牌图标，不运行发布者控制的服务器，不实现账号、认证或持久化。主要安全目标是避免发布包包含敏感材料、设备路径、访问凭据或超出学习辅助范围的危险指令。

## 报告漏洞

优先使用 GitHub 仓库 **Security** 页面中的私密漏洞报告入口。若该入口不可用，请创建不含敏感细节的 Issue，请求发布者提供私密沟通方式。不要把未公开材料、访问凭据或可直接利用的细节放入公开 Issue。

报告应包含受影响版本、复现条件、潜在影响和建议修复方向。发布者会在合理时间内确认并评估报告。

## 发布要求

每次发布必须通过允许列表打包、敏感残留扫描、插件结构校验、12 个 Skill 校验和路由场景检查。发布流程禁止强制推送。
