# 安全政策

## 支持版本

| 版本 | 支持状态 |
| --- | --- |
| `1.2.x` | 当前支持 |
| `1.1.x` | 历史版本，仅保留 Release |
| `1.0.x` | 历史版本，仅保留 Release |

安全修复会优先在最新受支持版本中发布。

## 架构边界

本项目只发布静态 Skills、共享契约与品牌图标，不运行发布者控制的 App、MCP、服务器、账号、认证、模型代理或持久化服务。主要安全目标是避免仓库与发布包包含私有材料、设备路径、访问凭据、危险指令或超出学习辅助范围的能力声明。

## 报告漏洞

优先使用 GitHub 仓库 **Security** 页面中的私密漏洞报告入口。若该入口不可用，请创建不含敏感细节的 Issue，请求发布者提供私密沟通方式。不要把未公开材料、访问凭据、个人信息或可直接利用的细节放入公开 Issue。

报告应包含受影响版本、复现条件、潜在影响和建议修复方向。发布者会在合理时间内确认并评估报告。

## 发布要求

每次发布必须满足以下门禁：

- 使用完整路径允许列表打包，拒绝额外文件、符号链接、路径穿越与重复 ZIP 成员；
- 扫描完整 Git 历史中的私钥、Token、API Key、JWT、本机路径与旧系统残留；
- 通过插件结构校验、12 个 Skill 校验、JSON Schema 校验和变异测试；
- 动态路由评测达到 `60/60`，多轮行为评测达到 `24/24`，且证据哈希与当前插件树、用例和评测器匹配；
- 使用维护者离线密钥对规范化评测声明生成 OpenSSH 分离签名；私钥不得进入仓库或 CI；
- 由受保护基分支中的可信验证器和候选 checkout 之外固定的 `allowed_signers` 公钥验证证据；候选仓库内的公钥或仅通过内部一致性检查的证据不得作为发布授权；
- 签名必须锁定源提交、插件树、测试集、评测器、完整证据字节和结构化响应摘要清单，且有效期不得超过 30 天；
- Windows 与 Ubuntu 构建生成字节一致的 ZIP 和相同 SHA-256；
- 通过受保护分支和 tag workflow 发布，禁止强制推送或改写已有版本标签。
