# v1.1.0 动态前向评测报告

本报告由 `evals/run_forward_eval.py` 根据本地隔离评测结果生成。

当前状态：尚未针对最终提交运行。发布门禁要求先提交稳定的插件树与测试集，再运行 36 个路由场景和 12 个多轮行为场景；生成的机器证据必须与当前插件树、测试集哈希一致，并由 `evals/verify_forward_evidence.py` 验证为 36/36 与 12/12。
