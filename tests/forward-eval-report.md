# v1.1.0 动态前向评测报告

本报告由 `evals/run_forward_eval.py` 根据本地隔离评测结果生成。

当前状态：尚未针对最终提交运行。发布门禁要求先提交稳定的插件树与测试集，再运行 36 个路由场景和 12 个多轮行为场景；生成的机器证据必须与当前插件树、测试集和评测器哈希一致，并达到 36/36 与 12/12。

`evals/verify_forward_evidence.py` 只验证仓库内证据的一致性，不认证模型运行来源。正式门禁还要求 `evals/forward_attestation.py` 生成逐响应摘要清单和规范化声明，由维护者离线密钥签名，再由受保护基分支中的可信验证器使用候选 checkout 之外固定的公钥验证。
